{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"description": "Schema for the agent",
	"type": "object",
	"title": "AgentInfo",
	"required": [
		"name",
		"agent_description",
		"active_subreddit",
		"behavior"
	],
	"properties": {
		"name": {
			"type": "string",
			"description": "The name of the agent"
		},
		"agent_description": {
			"type": "string",
			"description": "A description of the agent"
		},
		"active_subreddit": {
			"type": "string",
			"description": "The subreddit the agent is active in. Exclude the r/ prefix."
		},
		"behavior": {
			"type": "object",
			"description": "The behavior of the agent",
			"required": [
				"reply_needed_classification",
				"reply_style",
				"minimum_comment_age_minutes",
				"minimum_time_between_submissions_hours"
			],
			"properties": {
				"reply_needed_classification": {
					"type": "string",
					"description": "Explain what kinds of comments the agent should reply to, and what kinds it should ignore."
				},
				"reply_style": {
					"type": "string",
					"description": "The style of the agent's replies."
				},
				"minimum_comment_age_minutes": {
					"type": "integer",
					"description": "The minimum age of a comment in minutes before the agent will reply to it."
				},
				"minimum_time_between_submissions_hours": {
					"type": "integer",
					"description": "The minimum time since the agent's last submission in hours before it will make another submission."
				}
			}
		}
	}
}